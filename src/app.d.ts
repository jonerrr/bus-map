// import type { FeatureCollection, MultiLineString, Point } from 'geojson';
// import type { Trip, Route, Stop } from '$lib/types';

// See https://kit.svelte.dev/docs/types#app
// for information about these interfaces
declare global {
	namespace App {
		// interface Error {}
		// interface Locals {}
		// interface PageData {
		// 	trips: FeatureCollection<Point, Trip>;
		// 	stops: FeatureCollection<Point, Stop>;
		// 	routes: FeatureCollection<MultiLineString, Route>;
		// }
		// interface PageState {}
		// interface Platform {}
	}
}

export {};
